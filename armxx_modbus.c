/*
* capture data from canbus or rs485
*
*/
#include"config.h"
#include"commond.h"

int GetSensorData(struct Sensor_Data sensordata)
{
	//sensor network protocol

	//check the sensor network status

	//send commond to sensor node

	//recv data from sensor node

	//libmodbus is used to communication between the sensor node and hosts.
 
	return 1;
}
